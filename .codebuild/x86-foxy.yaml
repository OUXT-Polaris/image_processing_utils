version: 0.1

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - cd ../ && ls
  post_build:
    commands:
      - echo Build completed on `date`
